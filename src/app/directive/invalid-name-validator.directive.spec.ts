import { InvalidNameValidatorDirective } from './invalid-name-validator.directive';

describe('InvalidNameValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidNameValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
